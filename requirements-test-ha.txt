# Dependencias para los tests P2 con harness de Home Assistant (config flow,
# setup/unload, entidades). Se ejecutan vía Docker (ver Dockerfile.test /
# docker-compose.yml), no en la máquina del desarrollador.
#
# pytest-homeassistant-custom-component fija internamente la versión de
# `homeassistant` que instala, y con ella la de Python (2026.9 exige >= 3.14.2).
# Mantener este pin al día: si se queda atrás, los tests validan contra un
# Home Assistant que ya nadie ejecuta y las deprecaciones pasan desapercibidas.
pytest-homeassistant-custom-component==0.13.364
requests>=2.31.0
websocket-client==1.8.0
