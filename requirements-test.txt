# Dependencias para los tests que NO requieren Home Assistant (P0/P1).
# Ejecutar: pip install -r requirements-test.txt && pytest
pytest>=7.4
requests>=2.31.0
websocket-client==1.8.0
freezegun>=1.2

# Los tests P2 con harness de Home Assistant (config flow, setup/unload) NO se
# instalan aquí: requieren Python >= 3.12 y corren vía Docker para no tocar la
# máquina del desarrollador. Ver Dockerfile.test, docker-compose.yml,
# requirements-test-ha.txt y docs/testing-strategy.md.
